<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:MusicPlayerOnlineApp.Views"
       Title="MusicPlayerOnlineApp"
       x:Class="MusicPlayerOnlineApp.AppShell">

    <!--
        The overall app visual hierarchy is defined here, along with navigation.
    
        https://docs.microsoft.com/xamarin/xamarin-forms/app-fundamentals/shell/
    -->

    <Shell.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseStyle" TargetType="Element">
                <Setter Property="Shell.BackgroundColor" Value="#212121" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.TitleColor" Value="#FAFAFA" />
                <Setter Property="Shell.DisabledColor" Value="red" />
                <Setter Property="Shell.UnselectedColor" Value="SaddleBrown" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="#000000" />
                <Setter Property="Shell.TabBarForegroundColor" Value="Aquamarine"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="#757575"/>
                <Setter Property="Shell.TabBarTitleColor" Value="#0088EB"/>
            </Style>
            <Style TargetType="TabBar" BasedOn="{StaticResource BaseStyle}" />
            <Style TargetType="FlyoutItem" BasedOn="{StaticResource BaseStyle}" />
        </ResourceDictionary>
    </Shell.Resources>

    <TabBar>
        <ShellContent Title="播放列表" Icon="listen_to_music.png" ContentTemplate="{DataTemplate local:PlaylistPage}" />
        <ShellContent Title="我的歌单" Icon="music_folder.png" ContentTemplate="{DataTemplate local:MyFavoritePage}" />
        <ShellContent Title="设置" Icon="settings.png" ContentTemplate="{DataTemplate local:SettingPage}" />
    </TabBar>
</Shell>
